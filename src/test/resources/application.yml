spring:
  datasource:
    username: test
    password: pass
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/postgres
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: validate